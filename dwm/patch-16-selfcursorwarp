--- dwm.c.orig	2025-01-24 19:54:34.381246000 +0300
+++ dwm.c	2025-01-24 19:57:50.809484000 +0300
@@ -964,6 +964,7 @@
 
 void
 focusmon(const Arg *arg) {
+    int x, y;
     Monitor *m = NULL;
 
     if (!mons->next)
@@ -978,6 +979,10 @@
     if (m) {
         selmon = m;
         focus(NULL);
+        // Warp cursor to the center of the new monitor
+	x = m->wx + (m->ww / 2);
+	y = m->wy + (m->wh / 2);
+	XWarpPointer(dpy, None, RootWindow(dpy, screen), 0, 0, 0, 0, x, y);
     }
 }
 
@@ -1979,6 +1984,10 @@
 
     sendmon(selmon->sel, m);
     focus(NULL);
+    // Warp cursor to the center of the new monitor
+    int x = m->wx + (m->ww / 2);
+    int y = m->wy + (m->wh / 2);
+    XWarpPointer(dpy, None, RootWindow(dpy, screen), 0, 0, 0, 0, x, y);
     arrange(selmon);
 }
 
